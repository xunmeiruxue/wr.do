# =============================================================================
# WR.DO 环境变量配置文件
# =============================================================================
# 说明：
# 1. 复制此文件为 .env 并填写实际的配置值
# 2. 带 * 标记的为必填项，其他为可选项
# 3. URL 地址末尾不要添加斜杠 "/"
# =============================================================================

# -----------------------------------------------------------------------------
# * 应用基础配置 (必填)
# -----------------------------------------------------------------------------
# 应用访问地址 (生产环境需替换为实际域名)
NEXT_PUBLIC_APP_URL=http://localhost:3000
# 应用名称
NEXT_PUBLIC_APP_NAME=WR.DO

# -----------------------------------------------------------------------------
# * 数据库配置 (必填)
# -----------------------------------------------------------------------------
# PostgreSQL 数据库连接字符串
# 格式: postgres://用户名:密码@主机地址:端口/数据库名
# 示例: postgres://myuser:mypassword@localhost:5432/wrdo
DATABASE_URL='postgres://[user]:[password]@[hostname]:5432/[dbname]'

# -----------------------------------------------------------------------------
# * 认证配置 (必填)
# -----------------------------------------------------------------------------
# NextAuth.js 密钥 (生产环境必须修改为随机字符串)
# 生成方式: openssl rand -base64 32
AUTH_SECRET=abc123
# 认证服务地址 (通常与 NEXT_PUBLIC_APP_URL 相同)
AUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# OAuth 2.0 登录配置 (可选，至少启用一种登录方式)
# -----------------------------------------------------------------------------
# Google OAuth 登录
# 申请地址: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth 登录
# 申请地址: https://github.com/settings/developers
GITHUB_ID=
GITHUB_SECRET=

# LinuxDo OAuth 登录
# 申请地址: https://connect.linux.do/
LinuxDo_CLIENT_ID=
LinuxDo_CLIENT_SECRET=

# 通用 OAuth 2.0 / OIDC 配置 (支持 Authentik、Keycloak、Auth0 等)
# 如果配置了这些变量，将在登录页面显示自定义 OAuth 登录按钮
AUTH_OAUTH_ID=
AUTH_OAUTH_SECRET=
# OIDC Issuer 地址 (需包含 /.well-known/openid-configuration)
# Authentik 示例: https://your-authentik.com/application/o/wrdo/
AUTH_OAUTH_ISSUER=
# 登录按钮显示的名称，默认为 "OAuth"
AUTH_OAUTH_NAME=Authentik

# -----------------------------------------------------------------------------
# 电子邮件服务配置 (可选)
# -----------------------------------------------------------------------------
# Resend API 密钥 (推荐) - https://resend.com
RESEND_API_KEY=
# Brevo API 密钥 (备选) - https://www.brevo.com
BREVO_API_KEY=
# 发件人邮箱地址
EMAIL_FROM=service@wr.do
# 发件人显示名称
EMAIL_FROM_NAME=WRDO

# -----------------------------------------------------------------------------
# Webhook 推送配置 (可选)
# -----------------------------------------------------------------------------
# 启用 Webhook 邮件推送 (true/false)
# WEBHOOK_PUSH_ENABLED=false
# Webhook 推送地址
# WEBHOOK_URL=https://your-webhook-endpoint.com/api/email
# Webhook 签名密钥 (可选，用于 HMAC-SHA256 签名)
# WEBHOOK_SECRET=your-webhook-secret
# HTTP 请求方法 (POST 或 PUT，默认 POST)
# WEBHOOK_METHOD=POST
# 自定义 HTTP 请求头 (JSON 格式)
# WEBHOOK_HEADERS={"Authorization":"Bearer your-token"}
# 自定义 Payload 模板 (JSON 格式，可选)
# WEBHOOK_TEMPLATE=
# 推送白名单 (逗号分隔，留空则推送所有邮件)
# WEBHOOK_TARGET_WHITE_LIST=

# -----------------------------------------------------------------------------
# 对象存储配置 (可选)
# -----------------------------------------------------------------------------
# Cloudflare R2 自定义域名 (用于邮件附件等)
NEXT_PUBLIC_EMAIL_R2_DOMAIN=

# -----------------------------------------------------------------------------
# 数据分析配置 (可选)
# -----------------------------------------------------------------------------
# Google Analytics 追踪 ID
NEXT_PUBLIC_GOOGLE_ID=
# Umami 统计脚本地址
NEXT_PUBLIC_UMAMI_SCRIPT=
# Umami 网站 ID
NEXT_PUBLIC_UMAMI_WEBSITE_ID=

# -----------------------------------------------------------------------------
# 第三方服务配置 (可选)
# -----------------------------------------------------------------------------
# 截图服务 API 地址
SCREENSHOTONE_BASE_URL=https://shot.wr.do

# GitHub API Token (用于获取 Stars 等信息)
# 申请地址: https://github.com/settings/tokens
GITHUB_TOKEN=

# -----------------------------------------------------------------------------
# Docker 部署配置 (可选)
# -----------------------------------------------------------------------------
# 是否跳过数据库检查 (true/false)
# 首次部署建议设为 false，后续可改为 true 加快启动
SKIP_DB_CHECK=false
# 是否跳过数据库迁移 (true/false)
SKIP_DB_MIGRATION=false

# -----------------------------------------------------------------------------
# 其他配置
# -----------------------------------------------------------------------------
# 支持邮箱地址
NEXT_PUBLIC_SUPPORT_EMAIL=your_support_email@gmail.com